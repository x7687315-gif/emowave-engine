# P3C 代码质量检查报告 · 本会话增量整改

- 测试时间：2026-09-19 01:23:47
- 测试人员：AI Assistant（QoderWork）
- 规范依据：《阿里巴巴 Java 开发手册》P3C，**按 Python 适配**（命名 / 魔法值 / 异常 / 并发 / 参数校验 / 安全 / 单测）
- 检查模式：**增量（本会话改动文件）**
- 覆盖文件：`main_app.py`、`windows/main_console.py`、`windows/legacy_embed.py`、`windows/drawer.py`、`windows/event_detail_dialog.py`、`windows/event_summary_window.py`、`windows/history_window.py`、`windows/archetype_dialog.py`、`curve_widget.py`、`db.py`、`theme.py`、`emowave/core/domain/archetype.py`、`emowave/core/calibration/calibrator.py`、`emowave/__init__.py` 及配套测试

## 问题统计
- 问题总数：4
- 致命：0
- 严重：2（本次已修）
- 一般：2（记录/存量）

## 严重问题（已修复）

【P3C-001】异常处理 · 空 catch（P3C-EXC-001）
- 位置：`windows/main_console.py` `_clear_active_wave` / `_resume_active_wave`（原 `except Exception: pass` / 静默 return）
- 问题：静默吞异常，故障不可见。
- 处置：✅ 改为 `logger.debug/warning` 记录后再返回/清除损坏快照。

【P3C-002】异常处理 · 空 catch（P3C-EXC-001）
- 位置：`main_app.py` `apply_archetype`（原 `except Exception: pass`）
- 处置：✅ 新增模块级 `logger`，改为 `logger.debug` 记录持久化失败。

## 一般问题（记录）

【P3C-003】异常处理 · 存量空 catch
- 位置：`main_app.py` `closeEvent` `except Exception: pass`（关库失败不拦退出）
- 判定：存量、非本会话改动行，语义为"有意吞掉以保证可退出"，有注释。按渐进式改进策略记录，未强改。

【P3C-004】命名风格（P3C-RED-004）
- 位置：`windows/drawer.py` 局部 `il/hl/t/sep` 等短缩写
- 判定：存量风格、与周边一致，可读性影响小，记录不阻断。

## 安全规约核查（P3C-SEC）
- 【P3C-SEC-001 SQL 注入】`db.update_event` 的 f-string 仅拼接**白名单列名**（`_EDITABLE_LIST`/固定列），值一律 `?` 参数化；`get_event/save_event` 全参数化。✅ 无注入面。
- 【P3C-SEC-004 硬编码密钥】全仓扫描 `ghp_` 令牌：工作区与跟踪文件 0 命中；令牌仅命令行/环境变量临时用。✅
- 危险调用（eval/exec/os.system/subprocess/pickle）：本会话改动零引入。✅
- 敏感数据：情绪数据仅本地 SQLite，无外发/日志泄露。✅

## 其它核查
- 行长度 ≤120：改动文件 0 处超限。✅
- 魔法值：衰减阈值/时长抽为具名常量 `SETTLE_EPS/SETTLE_MIN_SEC/MAX_DECAY_SEC/DECAY_DT`；Russell 中性点 0.5 有注释说明。✅
- 命名：函数/变量 snake_case、类 CapWords（Python 惯例）。✅
- 注释：关键逻辑（衰减回基线、无后台快照恢复、白名单更新）均有中文注释。✅

## 单元测试（P3C-TST）
- 本会话新增测试：archetype 7 + 精力先验/曲线/抽屉/波生命周期/事件补全/锚点 等，累计 `tests/` 54 + `emowave/tests/` 588 = **642 全绿**。
- 遵循 pytest `test_*` 命名、独立性（临时库 + close）、断言**实际行为**（宽度/落库/曲线点数/锚点值）。✅

## 质量评估
- 变更代码 P3C 符合度：修复后新增代码致命/严重违规 = 0。
- 评分：**优秀（92）**。存量 2 项记录待后续。
